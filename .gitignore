# Large binaries — distributed separately, exceed GitHub's 100 MB file limit
misc_resources/
Lab Manual/assets/*.mp4

# Obsidian: share the vault config (theme, snippets, plugin settings) so the
# manual renders as intended. workspace.json is tracked on purpose, pinned
# to open 00 in reading view for students on a fresh pull; the working copy
# in the primary source vault is kept off the index with skip-worktree so
# local tinkering doesn't leak into commits. Mobile layout stays local.
.obsidian/workspace-mobile.json
.trash/

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor swap/backup files
*.swp
*~

# Claude Code local settings
.claude/settings.local.json
